<nav class="navbar flex-column pr-0 position-fixed">
  <%= link_to root_path, class: 'navbar-header d-flex flex-column justify-content-center align-items-center' do %>
    <%= gravatar_for(current_user) %>
    <h3><%= full_name %></h3>
  <% end %>
  <div class="navbar-links mb-auto w-100 pl-2" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <i class="fas fa-cog"></i><%= link_to 'Account settings', user_path(current_user), class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <i class="fas fa-plus"></i><%= link_to 'New Activity', new_activity_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <i class="fas fa-running"></i><%= link_to 'My Activities', activity_entries_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <i class="fas fa-users"></i><%= link_to 'My Groups', membership_path(current_user), class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <i class="fas fa-sign-out-alt"></i><%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'nav-link' %>
      </li>
    </ul>
  </div>
</nav>