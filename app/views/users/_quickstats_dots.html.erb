<div class="quick-stats-weekly-overview w-100 d-flex">
  <div class="d-flex flex-column w-100 quick-stats-weeks quick-stats-dots px-2">
    <div class="d-flex flex-row w-100 justify-content-center">
      <p class='d-inline-block mr-2'><%= (Date.today - 7).strftime('%B %e') %></p>
      <p class="d-flex-inline justify-content-start align-items-start h-100">
        <%= quick_stats_dots(@recent_activities7.count)%>
      </p>
    </div>
    <div class="d-flex flex-row w-100 justify-content-center">
      <p class='d-inline-block mr-2'><%= (Date.today - 14).strftime('%B %e') %></p>
      <p class="d-flex-inline justify-content-start align-items-start h-100">
        <%= quick_stats_dots(@recent_activities14.count)%>
      </p>
    </div>
    <div class="d-flex flex-row w-100 justify-content-center">
      <p class='d-inline-block mr-2'><%= (Date.today - 21).strftime('%B %e') %></p>
      <p class="d-flex-inline justify-content-start align-items-start h-100">
        <%= quick_stats_dots(@recent_activities21.count)%>
      </p>
    </div>
    <div class="d-flex flex-row w-100 justify-content-center">
      <p class='d-inline-block mr-2'><%= (Date.today - 28).strftime('%B %e') %></p>
      <p class="d-flex-inline justify-content-start align-items-start h-100">
        <%= quick_stats_dots(@recent_activities28.count)%>
      </p>
    </div>
  </div>
</div>